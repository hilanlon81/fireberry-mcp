import{McpServer}from"@modelcontextprotocol/sdk/server/mcp.js";import{StreamableHTTPServerTransport}from"@modelcontextprotocol/sdk/server/streamableHttp.js";import{z}from"zod";import axios from"axios";import express from"express";const FIREBERRY_TOKEN=process.env.FIREBERRY_TOKEN;const FIREBERRY_BASE_URL="https://api.fireberry.com/api";const app=express();app.use(express.json());app.all("/mcp",async(req,res)=>{const server=new McpServer({name:"fireberry",version:"1.0.0"});server.tool("getOpportunity",{id:z.string()},async({id})=>{const response=await axios.get(`${FIREBERRY_BASE_URL}/record/Opportunity/${id}`,{headers:{Token:FIREBERRY_TOKEN}});return{content:[{type:"text",text:JSON.stringify(response.data,null,2)}]};});const transport=new StreamableHTTPServerTransport({sessionIdGenerator:undefined});await server.connect(transport);await transport.handleRequest(req,res,req.body);});const PORT=process.env.PORT||3000;app.listen(PORT,()=>console.log(`Fireberry MCP running on port ${PORT}`));